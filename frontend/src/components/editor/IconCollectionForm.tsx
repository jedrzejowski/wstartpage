import React, {FC} from "react";

export const IconCollectionForm: FC<{
    iconCollectionName: string;
}> = React.memo(props => {


    return <>
        Kolekcja
        hej akup se kleja
    </>;
});

export default IconCollectionForm;
